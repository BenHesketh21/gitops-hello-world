---
# Source: hello-world-chart/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world-api-dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-world-api-dev
  template:
    metadata:
      labels:
        app: hello-world-api-dev
    spec:
      containers:
        - name: hello-world-api-dev
          image: "gitops-hello-world-frontend:latest"
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
