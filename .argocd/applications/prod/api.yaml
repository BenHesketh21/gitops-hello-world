apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-world-api-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/BenHesketh21/gitops-hello-world.git'
    targetRevision: env/prod/api
    path: output
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true